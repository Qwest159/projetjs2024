import categories from "/src/storage/categories.json";

// console.log(categories);
/**
 *
 * @param {HTMLElement} element
 * @returns {void}
 */

export const accordeon = (categories) => {
  return;
};
